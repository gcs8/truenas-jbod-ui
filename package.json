{
  "name": "truenas-jbod-ui-browser-qa",
  "private": true,
  "version": "0.22.1",
  "description": "Lightweight browser QA smoke tests for truenas-jbod-ui.",
  "scripts": {
    "test:unit": "node --test tests/js/*.test.js",
    "qa:ui": "playwright test",
    "qa:ui:headed": "playwright test --headed",
    "qa:ui:debug": "playwright test --debug",
    "qa:ui:install": "playwright install chromium",
    "qa:ui:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
